<?php

class FileManager{
  public function deleteseq(){

      //array_map('unlink', glob("images/seq/*.gif"));
    /*  
  if ($handle = opendir('temp/')) {

      while (false !== ($file = readdir($handle))) {
          if (filectime($file)< (time()-7200)) {  
            unlink($file);
          }
      }
    }
  }*/
  }
}
?>